<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Bootstrap × 98.css</title>

  <!-- Bootstrap core (grid, utilities, JS behaviors) -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- 98.css base styles -->
  <link rel="stylesheet" href="https://unpkg.com/98.css@0.1.21/dist/98.css">

  <!-- Boot98 theme: remaps Bootstrap classes to 98.css look -->
  <style>
    :root{
      --w98-bg:#c0c0c0;
      --w98-win:#ece9d8;
      --w98-text:#000;
      --w98-blue:#000080;
      --w98-border-light:#fff;
      --w98-border-mid:#808080;
      --w98-border-dark:#000;
      --w98-shadow: inset -1px -1px var(--w98-border-dark), inset 1px 1px var(--w98-border-light),
                    inset -2px -2px var(--w98-border-mid), inset 2px 2px #dfdfdf;
      --w98-sunken: inset 1px 1px var(--w98-border-dark), inset -1px -1px var(--w98-border-light),
                    inset 2px 2px var(--w98-border-mid), inset -2px -2px #dfdfdf;
      font-synthesis-weight:none;
    }
    body{ background:var(--w98-bg); color:var(--w98-text); font-family: "MS Sans Serif","Tahoma",system-ui,sans-serif; }

    /* WINDOWS: map .card to a 98.css window */
    .card{
      background: var(--w98-win);
      border: 0;
      box-shadow: var(--w98-shadow);
      border-radius: 0;
      padding: 0;
    }
    .card .card-header{
      background: linear-gradient(#0a20a0, var(--w98-blue));
      color:#fff;
      padding: 4px 8px;
      font-weight: 700;
      display:flex; align-items:center; justify-content:space-between;
      box-shadow: inset 1px 1px #0f36d1, inset -1px -1px #00134d;
      border-bottom: 0;
    }
    .card .card-body{ padding: 12px; }

    /* BUTTONS */
    .btn{
      border: none;
      border-radius: 0;
      background: var(--w98-win);
      box-shadow: var(--w98-shadow);
      color: var(--w98-text);
      padding: 2px 12px;
      line-height: 1.2;
    }
    .btn:active, .btn:focus:active{
      box-shadow: var(--w98-sunken);
    }
    .btn:focus{
      outline: 1px dotted #000;
      outline-offset: -4px;
    }
    .btn-primary{
      background: #dcdad5;
      color:#000;
    }
    .btn-secondary{ background:#e7e4db; }
    .btn-danger{ background:#ffcccc; }
    .btn-success{ background:#ccffcc; }

    /* FORMS */
    .form-control, .form-select, .form-check-input{
      border: none;
      border-radius: 0;
      background: #fff;
      box-shadow: var(--w98-sunken);
      color: #000;
    }
    .form-control:focus, .form-select:focus{
      outline: 1px dotted #000;
      box-shadow: var(--w98-sunken);
    }
    .form-check-input{
      width: 18px; height: 18px;
    }

    /* NAVBAR -> Title bar style */
    .navbar{
      background: linear-gradient(#0a20a0, var(--w98-blue));
      color:#fff;
      border: 0;
      border-radius: 0;
      box-shadow: inset 1px 1px #0f36d1, inset -1px -1px #00134d, var(--w98-shadow);
      padding: 4px 8px;
    }
    .navbar .navbar-brand, .navbar .nav-link{ color:#fff; }
    .navbar .nav-link.active{ text-decoration: underline; }

    /* TABS */
    .nav-tabs{
      border-bottom: none; gap: 4px;
    }
    .nav-tabs .nav-link{
      border: none; border-radius: 0; background: var(--w98-win); box-shadow: var(--w98-shadow);
      color:#000; padding: 4px 8px;
    }
    .nav-tabs .nav-link.active{
      background: #fff; box-shadow: var(--w98-sunken);
    }

    /* TABLES */
    .table{
      background:#fff; border-collapse: collapse; box-shadow: var(--w98-sunken);
    }
    .table th, .table td{ border: 1px solid #b5b5b5; padding: 4px 8px; }

    /* PROGRESS */
    .progress{
      background: #fff; box-shadow: var(--w98-sunken); border-radius:0; height: 20px;
    }
    .progress-bar{
      background: repeating-linear-gradient(90deg,#0a20a0,#0a20a0 6px,#1a3bd1 6px,#1a3bd1 12px);
      border-right: 1px solid var(--w98-border-dark);
    }

    /* MODALS look like windows */
    .modal-content{
      background: var(--w98-win);
      border: 0; border-radius: 0; box-shadow: var(--w98-shadow);
    }
    .modal-header{
      background: linear-gradient(#0a20a0, var(--w98-blue));
      color:#fff; border-bottom:0; padding:4px 8px;
    }

    /* Alerts */
    .alert{ border-radius:0; box-shadow: var(--w98-sunken); border:0; }
  </style>
</head>
<body class="p-3">

  <nav class="navbar navbar-expand">
    <a class="navbar-brand fw-bold" href="#">My First VB4 Program</a>
    <div class="ms-auto d-flex gap-2">
      <button class="btn btn-sm" aria-label="Minimize">_</button>
      <button class="btn btn-sm" aria-label="Maximize">▢</button>
      <button class="btn btn-sm" aria-label="Close">✕</button>
    </div>
  </nav>

  <div class="container mt-3">
    <div class="card mb-3">
      <div class="card-header">Window Title</div>
      <div class="card-body">
        <p>Hello, world!</p>
        <form class="row gy-2">
          <div class="col-md-6">
            <label class="form-label">Occupation</label>
            <input class="form-control" type="text" />
          </div>
          <div class="col-md-6">
            <label class="form-label">Rating</label>
            <select class="form-select">
              <option>5 - Incredible!</option><option>4 - Great!</option>
              <option selected>3 - Pretty good</option>
              <option>2 - Not so great</option><option>1 - Unfortunate</option>
            </select>
          </div>
          <div class="col-12 d-flex justify-content-end gap-2">
            <button type="button" class="btn btn-primary">OK</button>
            <button type="button" class="btn">Cancel</button>
          </div>
        </form>
      </div>
    </div>

    <ul class="nav nav-tabs mb-3">
      <li class="nav-item"><a class="nav-link active" href="#">Tab 1</a></li>
      <li class="nav-item"><a class="nav-link" href="#">Tab 2</a></li>
    </ul>

    <div class="alert alert-light">This is a very serious Windows 98-style alert.</div>

    <div class="progress" role="progressbar" aria-valuenow="45" aria-valuemin="0" aria-valuemax="100">
      <div class="progress-bar" style="width:45%"></div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
